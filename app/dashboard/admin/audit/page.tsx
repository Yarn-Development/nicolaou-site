import { AuditClient } from "./audit-client"

export const metadata = {
  title: "QC Audit | Nicolaou Maths",
  description: "Rapid question quality control and auditing",
}

export default function AuditPage() {
  return <AuditClient />
}
